name: practise pipeline

on:
  workflow_dispatch:

jobs:
  my_first_job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
    -  name: 1st task hello
       run: echo "hello sumit . kamcho?"
    -  name: 2nd task list file
       run: ls -a
       
